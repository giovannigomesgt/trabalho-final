1 - Criar o namespace:
    kubectl create namespace airflow
2 - Adicionar repo apache:
    helm repo add apache-airflow https://airflow.apache.org
3 - atualizar:
    helm repo update
4 - pesquisar
    helm search repo airflow
5 - instalar airflow
    helm upgrade --install airflow apache-airflow/airflow --namespace airflow --create-namespace -f airflow/override-values.yaml --debug --timeout 60m0s

6 - 

EXTRA:
6 - exportar arquivo de valores

-----------------------------------------
DESATIVAR:
helm delete airflow -n airflow
kubectl delete namespace airflow
kubectl delete svc --all -n airflow
kubectl delete pvc --all -n airflow
eksctl delete cluster --region=us-east-1 --name=datagio

kurbenetes 1.25.2

kubectl get svc -n airflow
kubectl get pvc -n airflow